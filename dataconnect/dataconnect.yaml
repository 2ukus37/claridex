specVersion: "v1"
serviceId: "claridx-diagnostic"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "claridx-diagnostic-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
